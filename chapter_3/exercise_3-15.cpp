#include <iostream>
#include <string>
#include <vector>

using namespace std;

int main() {
  vector<string> vecstring;
  for (string buffer; cin >> buffer; vecint.push_back(buffer))
    ;

  return 0;
}